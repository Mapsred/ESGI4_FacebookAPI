{% extends 'AppBundle:Admin:base.html.twig' %}

{% block title %}Accueil Admin{% endblock %}

{% block header %}Mes albums{% endblock %}

{% block breadcumb %}
    <ol class="breadcrumb">
        <li>
            <a href="{{ path('site_home', {'project_name': site.username}) }}">
                <i class="fa fa-dashboard"></i> Mon site
            </a>
        </li>
        <li class="active">Mes albums</li>
    </ol>
{% endblock %}

{% block body %}

    {% for album in site.oAuthUser.albums if site.oAuthUser.albums.count > 0 %}
        <div class="post">
            <div class="user-block">
                <span class="username">
                    <a href={{ path('admin_album', {'project_name': site.username, 'album_name': album.name}) }}>{{ album.name }}</a>
                </span>
                <span class="description">{{ album.pictures|length }} photos posteés </span>
            </div>
            {% if album.pictures|length > 0 %}
                <!-- /.user-block -->
                <div class="row margin-bottom">
                    <div class="col-sm-6">
                        <img class="img-responsive" src="{{ album.pictures.first.pictureLink }}" alt="Photo">
                    </div>
                    <!-- /.col -->
                    <div class="col-sm-6">
                        <div class="row">
                            <div class="col-sm-6">
                                {% if album.pictures.get(1) %}
                                    <img class="img-responsive" src="{{ album.pictures.get(1).pictureLink }}" alt="Photo">
                                {% endif %}
                                <br>
                                {% if album.pictures.get(2) %}
                                    <img class="img-responsive" src="{{ album.pictures.get(2).pictureLink }}" alt="Photo">
                                {% endif %}
                            </div>
                            <!-- /.col -->
                            <div class="col-sm-6">
                                {% if album.pictures.get(3) %}
                                    <img class="img-responsive" src="{{ album.pictures.get(3).pictureLink }}" alt="Photo">
                                {% endif %}
                                <br>
                                {% if album.pictures.get(4) %}
                                    <img class="img-responsive" src="{{ album.pictures.get(4).pictureLink }}" alt="Photo">
                                {% endif %}
                            </div>
                            <!-- /.col -->
                        </div>
                        <!-- /.row -->
                    </div>
                    <!-- /.col -->
                </div>
                <!-- /.row -->
            {% endif %}
        </div>
    {% else %}
        <p> Aucun album </p>
    {% endfor %}

{% endblock %}


